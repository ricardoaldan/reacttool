import * as React from 'react';
import * as ReactDOM from 'react-dom';
import {FrameWorkTileComponent} from './components/frameworktile'

ReactDOM.render(
  <FrameWorkTileComponent />,
  document.getElementById('root')
);
